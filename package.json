{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "start": "([ -f .env ] || cp .env.example .env) && touch database/database.sqlite && docker compose up -d --build && docker exec smarthead_php composer install && docker exec smarthead_php php artisan key:generate --ansi && docker exec smarthead_php php artisan migrate --seed --ansi",
        "dev": "vite",
        "build": "vite build"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    }
}
